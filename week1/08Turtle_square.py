# draw a square
# import turtle module and give it a short name (t)
# turtle is a module that allows us to draw shapes
import turtle as t

# set the length of the sides of the square
length = 40

# t.forward(length)   # move forward by length
# t.left(90)          # turn left by 90 degrees
# t.forward(length)
# t.left(90)
# t.forward(length)
# t.left(90)
# t.forward(length)
# t.left(90)

# t.exitonclick()
